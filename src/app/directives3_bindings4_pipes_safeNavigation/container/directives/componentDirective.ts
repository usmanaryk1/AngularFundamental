import { Component } from '@angular/core'

@Component({
    selector:' conponent-directive',
    template:`

    
    Component directive is a special kind of directive which have its own template associated to it.<br>
    <textarea rows=5 cols-10 disabled style="border: none;background-color:white;"> 
   
    @(directive decarator)Component({
        selector: 'structure-Directive',
        template: '
        '
        )}
    
    </textarea>
    
    


    `
})
export class componentDirectiveComponent{}